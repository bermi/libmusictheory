# Graph Samples

These SVG files are generated from the current Zig renderer implementation.

Regenerate with:

```sh
zig run export_graph_samples.zig
```

Notes:

- `compat-*` files come from `src/harmonious_svg_compat.zig` API paths.
- `core-*` files come from direct core renderer methods in `src/svg/*.zig`.
